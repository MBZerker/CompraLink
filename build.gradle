plugins {
    id "com.android.application" version "8.7.3" apply false
    id "com.github.triplet.play" version "3.12.1" apply false
}

allprojects {
    buildDir = file("${System.getenv("TEMP")}/compralink-gradle-build/${project.path.replace(':', '_')}")
}
